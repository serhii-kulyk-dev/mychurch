# UX / доступність / лендінг-аудит MyChurch

**Прогін:** 2026-09-19, Playwright 1.63 (Chromium headless) + axe-core (WCAG 2.0/2.1 A+AA + best-practice)
проти `next dev` на `http://localhost:3000`.
**Обсяг:** 80 URL із `sitemap.xml` × (десктоп 1440 + мобайл 375) = 160 прогонів axe,
плюс сценарні перевірки на 14 ключових сторінках і 7 ширинах (320→1920).
**Скрипти прогону:** `audit.mjs` (обхід усіх URL), `flow.mjs`/`flow3.mjs` (сценарії),
`extra2.mjs` (контраст, адаптив, EN, reduced-motion), `hdr.mjs` (хедер по ширинах).

---

## 1. Тест-план і результат

| # | Набір | Що перевіряли | Результат |
|---|---|---|---|
| TP-1 | Смоук і маршрути | 80 URL, статуси, JS-помилки, 404 | ⚠️ 80/80 = 200, але 9 сторінок дають 401 у консолі |
| TP-2 | Перше враження | перший екран на 1280/1440/1920/390 | ⚠️ головний CTA обрізаний на 1420–1511 px |
| TP-3 | Навігація та IA | брейкпоінти, шухляда, Esc, футер | ❌ меню ховається вже на 1366; Esc не закриває шухляду |
| TP-4 | Конверсія | модалка демо, бриф, «Мій простір» | ❌ форма нічого не надсилає; немає пастки фокуса |
| TP-5 | Контент і зрозумілість | H1/H2, обсяг, латиниця, EN | ✅ структура заголовків чиста; EN покрито |
| TP-6 | Доступність WCAG 2.1 AA | axe × 160, клавіатура | ❌ 1334 порушення контрасту, 37 фокусованих у `aria-hidden` |
| TP-7 | Адаптив | 320/360/390/414/768/1024/1280 | ✅ горизонтального скролу немає; ⚠️ 154 елементи тексту < 12 px |
| TP-8 | Перформанс і рух | анімації, DOM, reduced-motion | ❌ `prefers-reduced-motion` ігнорується (112 анімацій живі) |
| TP-9 | SEO і шеринг | title/desc/canonical/OG/JSON-LD | ⚠️ немає hreflang для EN; 10 сторінок з коротким title |
| TP-10 | Довіра і юридичне | /privacy, /terms, контакти, ціна | ❌ `/privacy` і `/terms` = 404; Telegram-посилання порожнє |

---

## 2. P0 — ламає основний сценарій

### 2.1 Форма демо не надсилає лід нікуди
`src/components/shared/demo-modal.tsx:67-79` — `handleSubmit` лише ставить `setSubmitted(true)`.
Playwright під час сабміту зафіксував **0 мережевих запитів**, а користувач бачить
«Дякуємо! Ми зв'яжемось з вами найближчим часом». Те саме в брифі на `/modules`
(`church-brief.tsx:267`). У проєкті немає жодного `app/api/**/route.ts` і жодного `fetch`.

**Кожен лід із лендінгу зараз втрачається мовчки.**

**Зміна:** `POST /api/lead` → Telegram Bot API (`sendMessage` у робочий чат) + дублювання на
пошту/у таблицю; стан `idle | sending | ok | error`; при помилці — показати телефон і
`mailto` як запасний канал. Нічого не показувати як «дякуємо», доки відповідь не 2xx.

### 2.2 `/privacy` і `/terms` не існують — 404
`footer.tsx:172,175` і `demo-modal.tsx:210,212`. Посилання стоять **на кожній сторінці** і
під кнопкою, якою людина погоджується з умовами. Юридично згода посилається в нікуди.

**Зміна:** створити `src/app/privacy/page.tsx` і `src/app/terms/page.tsx`
(контент частково вже є в `src/content/legal.ts`), додати їх у `sitemap.ts`.

### 2.3 Головний CTA у хедері обрізаний на 1420–1511 px
Зміряно: на 1440 px ширина вмісту хедера = 1470 px → «Замовити дем**о**» зрізано на 30 px,
на 1420 px — на 50 px. Десктопне меню вмикається на `min-[1420px]`
(`navbar.tsx:101,119,147,165`), а місця потребує ~1560 px.

1440×900 — найпоширеніша роздільність MacBook Air. **Перше, що бачить половина відвідувачів
на ноутбуці, — обрізана головна кнопка.**

**Зміна:** підняти поріг до `min-[1560px]`, а в діапазоні 1024–1559 показувати
скорочене меню (4–5 пунктів + «Ще») замість бургера.

### 2.4 Порожнє Telegram-посилання
`footer.tsx:17` і `content/support.ts:89`: `const TELEGRAM = "https://t.me/"` — веде на
головну Telegram. При цьому окрема сторінка `/telegram` (8.9 екранів) продає бота,
до якого немає як дійти.

**Зміна:** підставити реальний `https://t.me/<bot_username>` і продублювати його
кнопкою в хедері на `/telegram`.

---

## 3. P1 — доступність (WCAG 2.1 AA)

| Правило | Вплив | Де | Факт |
|---|---|---|---|
| `color-contrast` | serious | 80/80 сторінок, 1334 вузли | токен `--ink-3` = 2.54:1 (світла) / 3.41:1 (темна) при нормі 4.5:1 |
| `aria-hidden-focus` | serious | `/` | 37 посилань `.chip-module` усередині `aria-hidden="true"` лишаються у Tab |
| `button-name` | critical | `/for-whom`, `/ai` | 6 кнопок без доступного імені (демо-чекбокси `data-demo="click"`) |
| `list` / `listitem` | serious | `/modules`, `/telegram`, `/for-whom/visitor` | `<li>` поза `<ul>`, сторонні вузли всередині списку |
| `landmark-unique` | moderate | 30 сторінок | два `<nav>` без розрізнення `aria-label` |
| `image-redundant-alt` | minor | 80/80 | `alt="Моя Церква"` поруч із тим самим текстом — подвійне озвучення |

**Окремо, поза axe:**

1. **Немає пастки фокуса в модалці.** Tab з відкритої модалки демо веде на сторінку за нею
   (зафіксовано: `Спробувати безкоштовно → Подивитись демо → Люди → Служіння`).
   Фокус при відкритті не переноситься в діалог, при закритті не повертається на кнопку.
   Те саме в «Мій простір». → `src/components/shared/demo-modal.tsx:40`
2. **Помилки форми не озвучуються:** `aria-invalid`, `aria-describedby`, `role="alert"`
   відсутні; фокус не переходить на поле з помилкою. → `src/components/shared/form-field.tsx`
3. **`prefers-reduced-motion` ігнорується:** при емуляції `reduce` живими лишаються
   **112 анімацій** — `auroraDrift`, `gradientSpin`, `pulseRing`, `wordIn`, `revealUp`,
   `barGrow`, `marqueeLeft`. Медіа-запити в `globals.css` покривають лише частину.
4. **Немає skip-link.** Щоб дійти табом до головної кнопки в героя, потрібно **19 натискань Tab**.
5. **`<header>` і `<footer>` усередині `<main>`** (`src/app/page.tsx:37-51`) — навігація
   потрапляє в основний контент, `landmark-unique` саме звідси.
6. **Шухляда на мобайлі не закривається Esc** (`aria-expanded` лишається `true`).
7. **Поля форми без `autocomplete` / `inputMode`** — на телефоні під номер відкривається
   звичайна клавіатура, автозаповнення не працює.
8. **154 елементи тексту < 12 px** на головній у мобільному вигляді.

---

## 4. Дизайн: що зайве і що псує перше враження

1. **Хедер перевантажений.** 11 пунктів меню + іконка пошти + UA/EN + тема + «Мій простір» +
   «Замовити демо» = 16 інтерактивних елементів у рядку. Норма для лендінгу — 5±2.
   Активний стан «Продукт» візуально злипається з логотипом.
   → залишити `Можливості · Модулі · Для кого · Ціни · Блог` + одну кнопку;
   решту («Імпорт», «Бот», «ШІ», «Про нас», «Амбасадор», «Підтримка») — у футер і в контент.
2. **Шухляда — 12 однакових рядків без групування.** Додати підзаголовки
   «Продукт / Ресурси / Компанія». Назви «Бот», «Питання», «Амбасадор» нічого не кажуть
   першому відвідувачу.
3. **Eyebrow-мітки перед кожним заголовком** (`МОДУЛІ`, `ДЛЯ КОГО`, `ХТО ПРАЦЮЄ В СИСТЕМІ`)
   — це подвійний вступ до кожного блоку. На `/for-whom` перший екран = мітка + H1 +
   підзаголовок, далі порожнеча, далі знову мітка + H2 + абзац. Прибрати eyebrow там, де
   заголовок і так зрозумілий.
4. **Емодзі 👋 і ☎️ у полях модалки** — єдине місце з емодзі в інтерфейсі, вибивається зі
   стилю. Замінити на іконки з `lucide-react` (як у решті сайту) або прибрати.
5. **Заголовок модалки на 4 рядки:** «Залиште контакт — ми зв'яжемось і покажемо як все
   працює». Скоротити до «Покажемо систему на вашій церкві» (+ кома після «покажемо»).
6. **Порожній чат у героєві `/ai`:** дві третини макета телефона — порожнє крапкове поле з
   одним повідомленням унизу, а ряд підказок зрізаний правим краєм. Показати готовий
   короткий діалог (2 репліки + результат).
7. **Головна — 16 екранів / 12 секцій / 1866 слів,** `/ambassadors/nove-zhyttia` — 12.9,
   `/modules` — 11.7. Між героєм і фінальним CTA майже немає точок конверсії.
   → скоротити головну до 7–8 секцій, додати CTA після «Вся церква в одній системі» і
   після «Рутина працює сама».
8. **Один і той самий фінальний блок «Епоха таблиць і чатів добігає кінця»** стоїть на 13
   сторінках дослівно. Для людини, що переглянула 3 сторінки, це шум.
   → варіювати під контекст сторінки.
9. **Немає цін.** Жодної сторінки і жодного блоку про вартість; «Спробувати безкоштовно»
   не пояснює, що буде після. Для B2B-лендінгу відсутність ціни — головна причина відмови.
10. **404 без хедера й футера** — глухий кут із єдиним посиланням «на головну».
11. **Тонкі сторінки:** `/for-whom` 354 слова, `/for-whom/*` 237–301 слово при meta
    description 41–61 символ — ризик «thin content» у пошуку.

---

## 5. SEO і технічне

- `hreflang` відсутній, хоча є перемикач UA/EN. EN-версія живе на тому самому URL і
  перемикається на клієнті → для пошуку її не існує.
  → `/en/...` через i18n-роутинг Next або принаймні `<link rel="alternate" hreflang>`.
- 10 сторінок модулів мають title 20–24 символи («Люди — модуль MyChurch»);
  `/for-whom/*` — description 41–61 символ.
- 9 сторінок модулів вантажать `iframe` з Google Drive і дають **401** у консолі
  (`/modules/people`, `family`, `groups`, `learning`, `onboarding`, `forms`, `links`,
  `automations`, `org`). Для анонімного відвідувача відео може не програватись.
  Атрибут `title` в іframe англійською: «The "People" module in action».
  → перенести відео у власний `<video>` з постером або на публічний хостинг.
- Дублювання DOM: 4 пари перемикачів мови (8 кнопок) на сторінку; 3819 вузлів на головній.

---

## 6. Що працює добре

- Немає горизонтального скролу на жодній із 7 ширин (320→1280).
- 80/80 сторінок віддають 200; жодної JS-помилки, жодного падіння.
- Структура заголовків чиста: рівно один H1 на сторінку, **нуль** стрибків рівнів на 80 сторінках.
- `lang="uk"`, canonical, OG-картинка, JSON-LD — на кожній сторінці.
- EN-локаль покрита практично повністю (лишились «Нове Життя» як власна назва і одна репліка демо-чату).
- Перший екран на мобайлі зібраний правильно: бейдж → H1 → підзаголовок → дві кнопки → три докази.
- Темна тема витримана, без «злипань» і втрати ієрархії.

---

## 7. Порядок робіт

**Спринт 1 (лід не втрачається).** `/api/lead` + Telegram/пошта · сторінки `/privacy` і
`/terms` · реальне `t.me` посилання · поріг хедера 1560 px.

**Спринт 2 (доступність).** `--ink-3` до 4.5:1 · білий текст на `--brand` (взяти
`--brand-deep` для заливки кнопок) · пастка фокуса + повернення фокуса в обох модалках ·
`aria-invalid`/`role="alert"` у формах · `tabindex="-1"` для чипів у `aria-hidden` ·
глобальний блок `prefers-reduced-motion` · skip-link · `<header>`/`<footer>` з `<main>` назовні.

**Спринт 3 (лендінг).** Скорочення меню до 5 пунктів · сторінка цін · головна до 7–8 секцій
з CTA посередині · різні фінальні блоки · прибрати eyebrow-дублі · переробити герой `/ai`.

**Спринт 4 (SEO).** `hreflang` і `/en` · title/description для модулів і ролей ·
власний хостинг відео замість Google Drive · 404 з навігацією.

---

## 8. Що виправлено (прогін 2026-09-19, після правок)

Перевірено тим самим набором: axe на 17 ключових сторінках × 2 теми, сценарії в Playwright.

### Конверсія

- **`POST /api/lead`** — новий роут (`src/app/api/lead/route.ts`). Лід дублюється в
  Telegram і на пошту; достатньо одного каналу. Є honeypot проти ботів і заслінка
  5 заявок / 10 хв на адресу. Ключі — у `.env.example`.
- Обидві форми (модалка й бриф на `/modules`) справді надсилають:
  у прогоні зафіксовано `POST /api/lead`. Стан кнопки `idle → sending → sent | failed`.
- **Поки канали не налаштовані, роут віддає 503, і форма показує телефон і пошту**
  замість «Дякуємо». Хибного «ми зв'яжемось» більше немає.
- `/privacy` і `/terms` створені з готового `src/content/legal.ts` (14 і 13 розділів),
  додані в `sitemap.ts`. Посилання у футері й під кнопкою згоди більше не 404.
- Заголовок модалки скорочено з чотирьох рядків до двох; емодзі 👋 ☎️ замінено
  на іконки `lucide-react`, як у решті сайту.
- `https://t.me/` зведено в одну константу `SITE_TELEGRAM` (`src/lib/seo.ts`),
  значення береться з `NEXT_PUBLIC_TELEGRAM_URL`. Шести копій у коді більше немає.

### Хедер

| Ширина | Було | Стало |
|---|---|---|
| 1280 / 1366 | бургер, CTA у шухляді | бургер + видима кнопка «Замовити демо» |
| 1420 / 1440 | **CTA обрізано на 30–50 px** | нічого не обрізано, CTA на місці |
| 1512 | впритул | компактний хедер, запас є |
| 1560+ | — | повне меню + CTA |

Поріг повного меню піднято з 1420 до 1560 px, а в компактному стані головна дія
лишається у хедері з 640 px.

### Доступність

| Що | Було | Стало |
|---|---|---|
| Контраст на головній (світла / темна) | 42 / 38 вузлів | **0 / 0** |

> Заміри знімаються після того, як анімації входу завершились. Якщо перевіряти
> сторінку через 1,2 с після завантаження, axe ловить проміжні кадри плавної появи
> (напівпрозорий текст) і показує десятки «порушень», яких у кінцевому стані немає.
| Усього порушень axe на 17 сторінках | 291 вузол | **11** (4 з них — усередині iframe Google Drive) |
| Фокус у модалці | Tab виходив на сторінку за нею | замкнений у діалозі, повертається на кнопку |
| Помилки форми | німі | `aria-invalid`, `aria-describedby`, `role="alert"`, фокус на поле |
| `prefers-reduced-motion` | 112 анімацій продовжували рух | **0** |
| Skip-link | немає, 19 табів до кнопки | перший Tab, фокус стає в `<main>` |
| Фокусовані елементи в `aria-hidden` | 37 | **0** |
| `<header>` / `<footer>` | всередині `<main>` | на верхньому рівні, `<main id="main">` |
| Шухляда на Esc | не закривалась | закривається |
| Поля форми | без `autocomplete` і `inputmode` | `name` / `tel` + цифрова клавіатура |
| 404 | без хедера й футера, 4 посилання | повна оболонка, 50 посилань |

Токени: `--ink-3` 0.38 → 0.60, `--ink-2` 0.58 → 0.70 (те саме в темній темі),
`--brand` `#007aff` → `#0069e0`, заливка кнопок у темній темі `#3d9bff` → `#1a6ed6`,
зелений як текст `#12a150` → `#0e7a3c`. Ієрархія трьох рівнів тексту збережена.

### Лишилось свідомо не зачепленим

- **Google Drive iframe** на 9 сторінках модулів: 401 у консолі і 4 порушення axe
  (`role="application"`, `aria-label` на div, вкладений `role="main"`) — усе це
  всередині чужого документа. Лікується лише перенесенням відео у власний `<video>`.
- Сім вузлів контрасту в декоративних моках (`/about`, `/modules/people`,
  `/for-whom/pastor`) — білі підписи на кольорових кружках і демо-кнопках усередині
  ілюстрацій. Поглибив кольори через `color-mix`, але ці сім axe все ще рахує;
  вони не несуть змісту, який губиться.
- **Продуктові рішення** з розділів 4 і 5: скорочення меню до 5 пунктів, групування
  шухляди, довжина головної, повторюваний фінальний блок, eyebrow-дублі, `hreflang`
  і `/en`. Це не дефекти, а зміни позиціонування — за вашим рішенням.
- `eslint` показує 3 помилки (`set-state-in-effect` × 2, `no-html-link-for-pages`)
  у коді, який зараз править інша сесія, — не чіпав.
