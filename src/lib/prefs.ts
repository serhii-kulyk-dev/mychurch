/* Plain constants shared by the server layout (boot script) and the client
   stores. Must stay free of "use client" so the layout gets real strings. */
export const THEME_KEY = "mychurch-theme";
export const LANG_KEY = "mychurch-lang";
